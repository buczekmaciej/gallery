{% extends 'base.html.twig' %}

{% block title %}
	Login
{% endblock %}

{% block stylesheets %}
	{{parent()}}
	<link href="/styles/user/login.css" rel="stylesheet">
{% endblock %}

{% block body %}
	<form action="{{ path('login') }}" method="POST">
		<p class="tag">
			Sign in
		</p>
		<div class="inpBox">
			<input type="text" id="username" name="username" value="{{ username }}" autofocus/>
			<label for="username">
				Username
			</label>
		</div>
		<div class="inpBox">
			<input type="password" id="password" class="pass" name="password" value=""/>
			<label for="password">
				Password
			</label>
			<span class="showBtn">
				Show
			</span>
		</div>
		<label>
			<input type="checkbox" name="_remember_me">
			Remember me
		</label>
		<a href="">
			Forgot password?
		</a>
		<input type="hidden" name="csrf" value="{{ csrf_token('authenticate') }}">
		<button id="submit-login" type="submit">
			Sign in
		</button>
		<a href="">
			Become member now
		</a>
		{% if error %}
			<div class="alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}
	</form>
{% endblock %}
